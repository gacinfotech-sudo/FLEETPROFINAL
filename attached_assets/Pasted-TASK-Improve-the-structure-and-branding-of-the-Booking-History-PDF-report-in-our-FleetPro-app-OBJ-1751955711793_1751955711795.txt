TASK: Improve the structure and branding of the Booking History PDF report in our FleetPro app.

OBJECTIVE:
1. **Fetch and Display Company Branding**
   - Add the **company logo**, **company name**, and **address** at the top of each page.
   - These details must be fetched dynamically from the company’s **Business Profile** section (as already implemented for invoice generation).
   - Make sure the company logo is shown on the left, company name centered, and address beneath the name.
   - Use a clean and professional layout.

2. **Currency Format**
   - Replace any strange symbols with a proper **"₹" (INR)** symbol for all monetary values.

3. **Design and Structure Enhancements**
   - Ensure proper column alignment, padding, and clear font styling.
   - Clearly define **headers** (Booking ID, Customer, Phone, etc.) with bold styling.
   - Use alternating row colors for better readability (optional but preferred).
   - Keep page numbers and generation timestamp at the bottom.

4. **PDF Export Behavior**
   - The exported PDF should be structured, paginated, and styled properly for readability.
   - It must reflect the same data but in a clean and polished design (as per the user’s mockup PDF format).

5. **Conditions**
   - DO NOT change any existing functionality or logic.
   - DO NOT alter the database.
   - Ensure compatibility with all current features.
   - No breaking changes allowed.
   - Maintain responsive PDF structure.

6. **Test Scenarios**
   - Check if the PDF reflects details correctly when generated by sub-users (e.g. managers).
   - Ensure that branding elements show only for the corresponding company that owns the booking data.

IMPLEMENTATION NOTE:
- The design should reflect 3+ pages as per user booking volume.
- Continue using the existing data source and logic for fetching booking history; only change the PDF generation layout and branding.

ACTION: Implement the above styling and branding into the Booking History PDF export. Keep all core features and codebase intact.
