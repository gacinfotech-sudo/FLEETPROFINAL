# Dashboard — Click Action Matrix

Per the brief's format: label, data source, click destination, applied filter, required permission, empty/loading/error behaviour. Covers the `dashboard` (Overview) view only. Rows marked **[Phase 1]** are implemented in this patch; rows marked **[Deferred]** are reported as out-of-scope for a safe additive patch and why.

| Element | Data source | Click destination | Filter applied | Permission | Empty state | Loading state | Error state | Status |
|---|---|---|---|---|---|---|---|---|
| Total Revenue card | `GET /api/dashboard/stats` | `/dashboard/revenue` | none (all-time total, matches existing card's own scope) | hidden unless `canViewRevenue()` (mirrors existing hide-for-manager) | n/a (always shows ₹0 if none) | existing skeleton | falls back to 0 silently (pre-existing, not changed) | **[Phase 1]** made clickable |
| Total Bookings card | `GET /api/dashboard/stats` | `/dashboard/history` | none (all-time total) | none beyond existing view access | n/a | existing skeleton | pre-existing silent fallback | **[Phase 1]** made clickable |
| Total Vehicles card | `GET /api/vehicles` | `/dashboard/fleet` | none | none beyond existing view access | n/a | existing skeleton | pre-existing silent fallback | **[Phase 1]** made clickable |
| Total Drivers card | `GET /api/drivers` | `/dashboard/drivers` | none | **only clickable for non-manager roles** — `drivers` view is already restricted for managers (`dashboard.tsx:101`); making the card clickable for a manager would navigate them into a route that immediately bounces back to the dashboard, a worse experience than a non-interactive card | n/a | existing skeleton | pre-existing silent fallback | **[Phase 1]** made clickable (role-conditional) |
| Upcoming Bookings card → Today tab | new `GET /api/dashboard/upcoming-bookings` | row click → existing `viewingBooking` "Booking Details" dialog (`dashboard.tsx:2028`) | pickup date/time within tenant-local today, status in the existing pre-dispatch set | existing dialog's own internal permission checks (unchanged) | "No bookings for today." + "Create Booking" button (reuses existing `handleViewChange("bookings")`) | new skeleton rows | new inline retry banner, rest of dashboard unaffected | **[Phase 1]** new |
| Upcoming Bookings card → Tomorrow tab | same endpoint | same | tenant-local tomorrow | same | "No bookings for tomorrow." + Create Booking | same | same | **[Phase 1]** new |
| Upcoming Bookings card → Future tab | same endpoint | same | pickup >= day after tomorrow, sorted nearest-first, capped at 200 with a "View All in Bookings" fallback link to `/dashboard/history` when truncated | same | "No bookings scheduled beyond tomorrow." + Create Booking | same | same | **[Phase 1]** new |
| Upcoming Bookings card → All Upcoming tab | same endpoint | same | today + tomorrow + future combined, same 200 cap/fallback | same | "No upcoming bookings." + Create Booking | same | same | **[Phase 1]** new |
| Quick Actions: Add Booking / Manage Fleet / Revenue Report | n/a (static) | `/dashboard/bookings` / `/dashboard/fleet` / `/dashboard/revenue` | n/a | existing | n/a | n/a | n/a | already working, unchanged |
| CEO Section A (Date Range / Brand / Branch / global filter bar) | would need new state + new date-range-aware `/api/dashboard/stats` params | n/a | n/a | n/a | n/a | n/a | n/a | **[Deferred]** — brand/branch has no underlying data model at all (see [MULTI_BRAND_BRANCH_AUDIT.md](./MULTI_BRAND_BRANCH_AUDIT.md)); a global date-range filter is safe in principle but touches every KPI card's backend query at once — sized as its own follow-up patch, not bundled into this one |
| Live Operations mini-board (Start Due / Delayed / Ending Soon / etc.) | `GET /api/operations/live-bookings` (already exists) | bucket click → `/dashboard/live-bookings` on the matching tab | bucket | n/a | n/a | new skeleton | new inline retry banner | **[Implemented]** — follow-up patch, done |
| Fleet/Driver status board (Available/On Trip/Maintenance counts) | derivable from already-fetched `/api/vehicles`/`/api/drivers` | segment click → `/dashboard/fleet` or `/dashboard/drivers`, filtered by status | status | n/a | n/a | n/a | n/a | **[Implemented]** — follow-up patch, done |
| Finance overview ("Today's Collection", Cash/UPI/Bank/Card/Other split) | new `GET /api/dashboard/finance-summary` — ledger-sourced (`PaymentTransaction`, `status: 'completed'`, today only), never a raw booking-field sum | whole card click → `/dashboard/revenue` (the existing Revenue Report; per-mode drill-down deferred — no distinct "Payments list" view exists yet to filter into) | none (today only, matches the card's own scope) | hidden unless `canViewRevenue()` (same hook the Total Revenue KPI card already uses) | n/a (shows ₹0 if none) | new skeleton | new inline retry banner | **[Implemented]** |
| Lead/booking source chart ("Booking Sources") | new `GET /api/dashboard/lead-sources` — real aggregation over `Booking.bookingSource`, sorted by count desc | bar click → `/dashboard/history`, filtered to bookings from that exact source | source | none beyond existing Booking History access | "No bookings yet." | new skeleton | new inline retry banner | **[Implemented]** |
| Alerts & Approvals panel | mostly doesn't exist (no approval-workflow entities in the schema) | n/a | n/a | n/a | n/a | n/a | n/a | **[Deferred — largely unsafe]** would require inventing new approval-workflow data models, not a dashboard-clickability patch; the parts that ARE real today (expiring documents — itself missing per [REAL_WORLD_WORKFLOW_GAPS.md](./REAL_WORLD_WORKFLOW_GAPS.md) Gaps 3–4, booking conflicts, driver leave conflicts already computed server-side) could be surfaced later once their underlying data exists |
| CEO 360 metrics (fleet utilization, vendor performance, brand/branch breakdowns) | mix of existing + entirely missing (vendor performance has no vendor ledger; brand/branch has no data model) | n/a | n/a | n/a | n/a | n/a | n/a | **[Deferred / partially unsafe]** — see safe change plan for the exact dependency blocking each sub-metric |
