# FleetPro Master Defect Register

Only defects with real evidence from this audit pass are listed. This is not a
consolidation of every defect mentioned across other sessions' documents (see
`docs/manual-preview/PENDING-LIVE-INTEGRATION.md` for that).

| ID | Severity | Domain | Problem | Reproducible | Root Cause | Affected Tenant/Data | Workaround | Fix Scope | Owner Worktree Recommendation | Regression Test | Release Blocker? |
|---|---|---|---|---|---|---|---|---|---|---|---|
| SA-01 | P1 | Auth/Runtime | Authenticated session intermittently dies (`401 "Authentication required"`/`"Invalid session"`) seconds after a successful login, under concurrent load. | Yes — reproduced twice this session during scripted API sequences. | NOT YET CONFIRMED. Correlated with the shared dev MongoDB's periodic idle-disconnect/reconnect cycle (visible in server logs, ~every 15 min) and/or heavy concurrent load from multiple simultaneous dev-server processes on one machine. Not proven to be a code defect vs. environment/session-store resilience gap. | Any tenant, session-store level, not data-specific. | Retry login. | Investigate session-store (connect-mongo?) resilience across a Mongo reconnect event; add reconnect-safe session reads. | `fleetpro-main` (Integrator) | Add a test that force-disconnects Mongo mid-session and asserts the session survives or fails gracefully with a clear message. | No — doesn't block manual testing, but should be fixed before real users rely on long sessions. |
| SA-02 | P2 | Release process | Three separate live preview builds exist simultaneously (`:5050`, `:5051`, `:5100`), each with a different, non-superset feature set. Confirmed live via direct HTTP checks at time of audit. | Yes — all three responded HTTP 200 at time of writing. | Multiple concurrent sessions each independently built their own reconciliation/integration branch without a single shared "canonical build" mechanism enforced at the tooling level. | N/A (process risk, not data risk). | Explicitly tell testers which port to use for which feature area until consolidated. | Pick one canonical worktree; diff the other two against it; cherry-pick/merge whatever unique work they have that the canonical one lacks; retire the other two. | Whoever is designated Integrator next | N/A (process, not code) | Not a code release blocker, but a real risk of wasted/misdirected manual testing. |
| SA-03 | ❓ unresolved discrepancy | Booking/Money | A different session's same-day report claims the money-calculation fix (`TASK-MONEY-ROOTCAUSE-01`, branch `money-rootcause`) is "materially incomplete (1 of 7 fields)" with "0/5 E2E pass." This audit's own direct API+DB test on `:5050`'s current commit found 6/6 exact money values with no drift. | Both findings are individually reproducible claims from real test runs, but against **different commits/branches** — not yet reconciled against the same commit. | NOT CONFIRMED — could be: (a) the other report tested an earlier/different commit where the fix genuinely was incomplete, (b) it tested the edit path or a UI click-through path this audit didn't test, or (c) an environment-contention false negative (that report's own text cites severe shared-environment instability, load average 38, at the time of its test). | N/A | N/A | Re-run the exact same money test (both this audit's direct-API method and the other session's UI-automation method) against the *same* single commit, in a quiet window, before trusting either finding as final. | Whoever owns money/booking next | The 5-value + 1-extra exact-arithmetic test this audit already wrote (reusable) | Yes, until reconciled — do not assume either finding is authoritative. |
| SA-04 | 🟢 informational, not a defect | GPS | GPS polling scheduler logs `"No documented adapter is registered for GPS provider 'official_docs_pending'"` repeatedly for many pre-existing seeded connections. | Yes, continuously in server logs. | Expected/by-design — the scheduler fails closed for unrecognized providers rather than crashing or fabricating data, exactly as its source task specified. | N/A | N/A | None needed unless those connections are meant to poll real data — then they need a real, documented provider assigned. | N/A | N/A | No. |

## Explicitly NOT filed as defects (checked, found working)

- Cross-tenant read via direct customer ID → correctly 404'd (not filed; this is the
  *absence* of a defect, listed here so it's clear it was actually tested).
- Cross-tenant write via direct booking ID (PUT) → correctly 404'd.
- Booking creation with exact required money values (6 cases) → all exact.
- Driver zero-block onboarding → confirmed non-blocking as designed.
