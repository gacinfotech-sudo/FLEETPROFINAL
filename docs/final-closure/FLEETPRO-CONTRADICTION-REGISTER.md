# FleetPro Contradiction Register

Reconciles `docs/master-closure/` (audited at trunk `20bd273`) against `docs/supreme-audit/`
(audited against `manual-test-preview`/`:5100` @ `2f31271`) and against **current** trunk
(`booking/integration-preview` @ `89a0454`, this pass's own direct verification). Every row
below was re-checked against current git ancestry, live routes, or the filesystem — not
inferred from either campaign's prose.

| ID | Feature/Claim | Campaign A (master-closure) | Campaign B (supreme-audit / PREVIEW-RUNTIME.json) | Commit A | Commit B | Current Trunk (89a0454) | Current Canonical Preview | Evidence Recheck | Final Verdict |
|---|---|---|---|---|---|---|---|---|---|
| CTR-001 | Which preview is canonical | `PREVIEW-RUNTIME.json`: `:5051` (`fleetpro-stable-demo`) is canonical | `FLEETPRO-EXECUTIVE-REAL-WORLD-AUDIT.md`: `:5100` (`manual-test-preview`) is canonical | `2599d48` | `2f31271` | Neither — trunk is a third, further-diverged lineage | Unresolved — 3 running processes, 3 different git lineages, confirmed live at 5050/5051/5100 | `git merge-base` from `fleetpro-main`: trunk↔5051 merge-base is `e17f9fb`; 5051↔5100 merge-base is `2f31271` (5100 is an ancestor of `2f31271`... — see CTR-002); trunk↔5100 has never converged | **CONTRADICTORY_EVIDENCE — both A and B are correct about their own claim being someone's "canonical" at some point, wrong that it's the only one.** No single canonical preview exists right now. See Fix Queue P0-1. |
| CTR-002 | Is `:5100` (manual-test-preview) "superseded" by `:5051` | `FLEETPRO-GAP-REGISTER.md` doesn't call it superseded — flags it as needing reconciliation | `PREVIEW-RUNTIME.json`'s own promotion history explicitly states "5100 (manual-test-preview) is behind this commit and should be treated as superseded, not a second Preview" | — | `2599d48`'s promotion note | N/A | `:5100` @ `168d205` has the entire **Root Control Plane batch** (7 tasks — domain/security/dashboard/sales-config/support + integration wave) that `:5051`/trunk do not have at all | Direct ancestry check: neither `168d205` nor `2599d48` is an ancestor of the other (confirmed `git merge-base --is-ancestor` both directions = NO). `:5100` has 22 real, unique, unmerged commits `:5051` lacks. | **CAMPAIGN_B_STALE (PREVIEW-RUNTIME.json's own note is wrong).** `:5100` is not superseded — it has real unmerged work `:5051` needs. Neither is a superset of the other. |
| CTR-003 | Driver 360 domain integration status | `findings-driver.md`: "confirmed fully unintegrated... not even wired into their own worktree's server" | (not directly addressed by supreme-audit) | `20bd273` (audit-time trunk) | — | `server/routes.ts` now calls `registerDriverDomainRoutes(app)`, `registerDriverDocumentModule(app)`, `registerVehicleHandoverRoutes(app)`; `Driver.lifecycleStage` schema field present; `GET /api/attendance/daily` registered; `driver-qa-foundations.spec.ts` present in `tests/e2e/` | Confirmed also present on `:5051` (`GET /api/drivers/:id/contacts` → 401, not 404/SPA-fallback) via direct `git merge-base --is-ancestor` of the underlying commits | Direct `grep`/`find` against current trunk source + live curl against `:5051` | **CAMPAIGN_A_STALE.** Driver domain/documents/handover/onboarding-UI/QA-security are integrated into both trunk and `:5051` as of this pass — this happened sometime after the master-closure audit ran (trunk `20bd273`), via direct re-application (not a merge of the original branch commits, hence ancestry checks against those exact SHAs still say NO, but the content is present under different commits). `:5100`'s status not independently re-checked but per CTR-002's ancestry, very likely also has it (shares the same pre-divergence history as `:5051`). |
| CTR-004 | GPS domain integration status (Fleet UI, QA-security, provider adapter, ingestion, billing) | `findings-gps.md`: "Neither worktree is reachable from trunk or Preview... runtimeRegistry.ts is deliberately empty" | (not directly addressed) | `20bd273` | — | `server/gps/providers/adapters/traccar/**` present and **registered** in `runtimeRegistry.ts` (`gpsProviderRegistry.register(TRACCAR_PROVIDER_KEY, createTraccarAdapter)`); `server/gps/ingestion/{pollingScheduler,webhookRoute,retry,deadLetter}.ts`, `server/gps/billing/{routes,reconciliationService}.ts`, `client/src/pages/gps-settings.tsx` + 15 GPS components, 17 GPS `tests/e2e/*.spec.ts` files all present | `:5051` confirmed to share this ancestry (same reasoning as CTR-003) | Direct `grep`/`find`/`cat` of `runtimeRegistry.ts` against current trunk | **CAMPAIGN_A_STALE.** The entire GPS domain (adapter registration included — "Test Connection" now works for Traccar-type connections, contrary to the audit's specific claim) is integrated into trunk and (per ancestry) `:5051`. |
| CTR-005 | Vehicle 360 domain integration status | `findings-vehicle.md`: "almost entirely unbuilt... nothing from this batch is merged into trunk or Preview" | Not audited (out of scope for supreme-audit's Phase 1) | `20bd273` | — | Fully built and merged: all 7 manifest tasks + 5 follow-ups (fleet-list link, GPS tab, manager permissions, Daily Inspections), commit `91c0c25`/`daab8e6`/`b1bce7b`/`89a0454` | **NOT present on `:5051` or `:5100`** — confirmed by ancestry (Vehicle 360's 14 commits are exclusively on trunk, none are ancestors of either preview) and live curl (`:5051/vehicles/<id>` → generic SPA shell, not the real page) | This session's own build-and-merge work (see `docs/vehicle-research/FINAL-VEHICLE-360-IMPLEMENTATION-REPORT.md`), independently re-confirmed via `git merge-base` and live curl this pass | **CAMPAIGN_A_STALE for "unbuilt"; CURRENTLY_BROKEN for "on any preview.**" Vehicle 360 is real and fully in trunk, but genuinely absent from both running previews — this is a real, current gap, not a stale finding. |
| CTR-006 | Telephony module integration status | `findings-telephony-whatsapp.md` / `findings-security-testinfra.md`: "server/telephony does not exist in trunk at all" | (not directly addressed, but supreme-audit's own canonical `:5100` predates telephony per master-closure's own ancestry table) | `20bd273` | — | **Confirmed still true**: `find server/telephony` returns nothing in current trunk; no `registerTelephonyRoutes` in `server/routes.ts` | Present on `:5051` (part of its unique 22-commit set — "Telephony (CallSession, multi-user RBAC...)" per `PREVIEW-RUNTIME.json`'s own promotion history); **not** present on `:5100` per master-closure's own ancestry table | Direct `find`/`grep` against current trunk (empty) | **CURRENTLY_VERIFIED (still accurate).** Telephony genuinely does not exist in trunk, genuinely does exist on `:5051`, genuinely does not exist on `:5100`. Not a stale finding — a real, current three-way gap. |
| CTR-007 | `qaclient` shared test login status (DEF-003) | N/A (master-closure didn't flag this) | `FLEETPRO-MASTER-DEFECT-REGISTER.md` DEF-003: "broken... 401 Invalid credentials... Root Cause: Not investigated" | — | reported ~19:20 IST today | Investigated this pass: the user's entire **tenant** (not just the user) was deleted at 19:23:39 IST today via an accidental `dropDatabase()` against the shared DB (see Fix Queue P0 for root cause). Independently confirmed via direct `mongod` log inspection. **A different concurrent session recreated `qaclient` at ~22:32 IST** (this pass verified: real login, 200, correct tenant, `mustResetPassword:false`) | Verified working via direct `curl` against trunk (`:5050`) at time of this report | Live `curl` test, `mongod.log` root-cause trace | **CURRENTLY_VERIFIED — RESOLVED.** DEF-003 was real and is now fixed (by a concurrent session, independently confirmed working by this pass). Root cause (shared-DB test isolation failure) is now understood and documented — see DB-002 in the Fix Queue. |
| CTR-008 | `bookingCode` (short code) integration status | `findings-booking-financial.md` BOOK-027: "zero references anywhere in trunk" | `AUDIT-DATABASE-INTEGRITY-findings.md` DB-001 discusses bookingCode uniqueness as if it's live somewhere | `20bd273` | — | **Confirmed still absent from current trunk** (`grep bookingCodeService server/routes.ts` → 0 hits) | Present on `:5051` and `:5100` (both have `feat/booking-code-02` merged, per their unique-commit lists) | Direct `grep` against current trunk (empty); ancestry confirms presence on both previews | **BOTH_PARTIAL — not a real contradiction once lineage is accounted for.** bookingCode is genuinely absent from trunk and genuinely present on both previews. DB-001's tenant-scoping concern (Fix Queue) applies to the previews' live implementation, not to trunk (which has none to scope). |
| CTR-009 | Money leading-zero bug (DEF-001/BUG-MONEY-001) status | `findings-booking-financial.md`: "not actually fixed anywhere... trunk fixes only 1/7 fields" | `FLEETPRO-MASTER-DEFECT-REGISTER.md`: fix `f97cff2` in `audit-fix-money` worktree, status `FIX_IN_WORKTREE`, **not** verified working | `20bd273` | `f97cff2` (unverified) | Confirmed still broken in current trunk: `grep "field.value || 0"` in `enhanced-booking-form.tsx` still matches multiple money fields (lines 2642/2698/2723/2743) | A live process is actively running in the `audit-fix-money` worktree right now (PID observed this pass, started ~21:41 IST) — a concurrent session is likely mid-fix | Direct `grep` against current trunk source | **CURRENTLY_VERIFIED (both campaigns agree, and it's still true).** Do not mark closed. A concurrent session appears to be actively working this right now — do not duplicate. See Fix Queue P1. |
| CTR-010 | RBAC role model (3 roles vs. 6 named roles) | `findings-security-testinfra.md` SEC-017: hard 3-role enum, real unresolved gap | Not directly addressed (AUDIT-AUTH-SECURITY-findings.md focuses on tenant-scoping, not role count) | `20bd273` | — | Confirmed still true: `IUser.role` is `'admin' \| 'client' \| 'manager'` in current `server/models/index.ts` | Same on all three lineages (not changed by any of the divergent work) | Direct `grep` against current trunk | **CURRENTLY_VERIFIED (still accurate, unresolved on every lineage).** |
